Download yolov8x-pose-p6.pt from google and put it in the same directory in which all other files are there 

Navigate to the directory in which your the project is run the python server.py Command in the Powershell to run and load all the AI models .
The backend models will load in the powershell and will run on the system's 5000 port number.

Open the project in pycharm and run the command python -m http.server 8000 to run the frontend on this port .

And after this , the project is running . 
 
It has various features like auto crop the photo , horizontally align the photo , check for eyes open or not , background is uniform or not , face is frontal or not etc 

it automatically removes the background of the signature which we uploaded and makes the background uniform .
